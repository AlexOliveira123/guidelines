import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('GuidelinesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GuidelinesPage(title: 'Guidelines')));
    //  final titleFinder = find.text('Guidelines');
    //  expect(titleFinder, findsOneWidget);
  });
}
